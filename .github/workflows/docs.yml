name: Docs

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  deploy:
    uses: hyperb1iss/shared-workflows/.github/workflows/docs-deploy.yml@v1
    with:
      engine: vitepress
      docs-dir: 'docs'
    secrets: inherit
